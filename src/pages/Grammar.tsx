import ContentManager from '../components/ContentManager';

export default function Grammar() {
  return (
    <ContentManager
      pageTitle="Grammar"
      pageDescription="Manage grammar topics, subtopics, and notes. Select a language and CEFR level to get started."
    />
  );
}
